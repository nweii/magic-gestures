# MagicGestures
#
# Every line that is not a comment is live. Write `setting = value`.
# Save this file, then pick Reload Settings from the menu bar to apply it.
#
# Reference: https://github.com/nweii/magic-gestures

[mouse]
hold-middle-tap-index = return

[trackpad]
hold-tap-left = return

[general]
show-menu-bar-icon = true
open-at-login = true
enable-mouse = true
enable-trackpad = true
tap-speed = 0.25
verbose-logging = false


# Examples
# ───────────────────────────────────────────────────────────────
# A # turns a line off. Remove it to switch one of these on, or add one
# above to park a setting without deleting it.
#
# These name their device up front, so they work anywhere in the file.
#
# mouse.hold-index-tap-middle = cmd+shift+a
# mouse.two-finger-tap = middle-click
# mouse.front-right-tap = escape
# trackpad.hold-tap-right = mission-control
# trackpad.three-finger-swipe-up = ctrl+up
#
# Full list: https://github.com/nweii/magic-gestures/blob/main/GESTURES.md
