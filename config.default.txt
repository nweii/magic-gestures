# MagicGestures
#
# Every line that is not a comment is live. Write `setting = value`.
# Pick Reload Gestures from the menu bar to apply a change.
# README.md in this folder lists every gesture, key, and setting you can use.

[mouse]
hold-middle-tap-index = return

[trackpad]
hold-tap-left = return

[general]
show-menu-bar-icon = true
open-at-login = true
enable-mouse = true
enable-trackpad = true
tap-speed = 0.25
verbose-logging = false
